{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna publish from-package --contents pkg"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {},
  "dependencies": {
    "lerna": "^3.15.0"
  }
}
